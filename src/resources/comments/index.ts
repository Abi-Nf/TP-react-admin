import { ShowGuesser } from 'react-admin';
import { Edit } from './edit';
import { List } from './list';

export const comments = {
  name: 'comments',
  list: List,
  edit: Edit,
  show: ShowGuesser,
};
